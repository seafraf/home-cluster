apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-server-series
  namespace: media-server
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Ti
  storageClassName: longhorn-hdd
